# Policy/Claim Lifecycle Persistence Automated Validation Plan

## A. Status

Status: TEST_PLAN_ONLY

Marker:

```text
POLICY_CLAIM_LIFECYCLE_PERSISTENCE_AUTOMATED_VALIDATION_PLANNED
```

No code is modified by this document.

No test is implemented by this document.

No app launch is authorized by this document.

## B. Baseline

Record:

- latest commit message:
  `docs(familyclaimref): record scenario9 runtime status and next validation sequence`
- source docs reviewed:
  - `docs/182_POLICY_CLAIM_RUNTIME_ROOT_PROVIDER_IMPLEMENTATION_RESULT_REVIEW.md`
  - `docs/189_POLICY_CLAIM_ISOLATED_RUNTIME_MANUAL_VALIDATION_RESULT_REVIEW.md`
  - `docs/191_POLICY_CLAIM_CURRENT_CORE_VALIDATION_STATUS_REVIEW.md`
  - `docs/192_POLICY_CLAIM_NEXT_CORE_VALIDATION_SEQUENCE_DECISION.md`

## C. Purpose

Record:

- isolated runtime root is now implemented and manually validated.
- next core risk is policy/claim lifecycle persistence.
- validation must prove created and disabled targets persist across service reload.
- validation must prove active target filtering excludes disabled targets.
- validation must not require UI redesign.

## D. Proposed Test Scope

Recommended test file:

- `tests/FamilyClaimRef.App.Tests/Integration/PolicyClaimLifecyclePersistenceTests.cs`
- repository-conventional equivalent is acceptable if test grouping changes before implementation.

Test cases:

1. Create policy and claim under isolated runtime root.
2. Recreate `AppServices` with same isolated root.
3. Verify policy and claim persist.
4. Disable policy target.
5. Disable claim target.
6. Recreate `AppServices` again.
7. Verify disabled targets persist as disabled.
8. Verify active policy list excludes disabled policy.
9. Verify active claim list excludes disabled claim.
10. Verify project root remains clean.
11. Verify default runtime evidence is not deleted.

## E. Synthetic Data Rules

Allowed:

- `policy_title_lifecycle_persistence_demo`
- `claim_title_lifecycle_persistence_demo`
- synthetic IDs generated by service

Forbidden:

- real personal data
- real insurer/hospital/diagnosis
- real policy/claim numbers
- `data/claimdoc` access

## F. Acceptance Criteria

Automated validation is acceptable only if:

- `dotnet build` passes.
- `dotnet test` passes.
- targeted lifecycle tests pass.
- no UI/XAML/resource files changed.
- no app launch occurred.
- no cleanup occurred.
- project root remains clean.
- `data/claimdoc` remains untouched.

## G. Planned Result Review

Future implementation batch must create:

- `docs/197_POLICY_CLAIM_LIFECYCLE_PERSISTENCE_AUTOMATED_VALIDATION_RESULT_REVIEW.md`

## H. Planning Judgment

```text
POLICY_CLAIM_LIFECYCLE_PERSISTENCE_AUTOMATED_VALIDATION_PLAN_READY
```
